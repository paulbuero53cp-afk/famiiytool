// Leerer Stub für den optionalen React-Native-Codepfad in jsmediatags
// (build2/ReactNativeFileReader.js). Wird im Browser nie ausgeführt
// (jsmediatags prüft zur Laufzeit die Umgebung), aber der Rolldown-
// Produktions-Build versucht den Import trotzdem statisch aufzulösen —
// siehe vite.config.ts (resolve.alias).
export default {};
